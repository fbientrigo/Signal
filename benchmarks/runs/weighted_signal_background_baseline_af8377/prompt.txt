Make a scientifically sound visualization that preserves the meaning of the supplied data. If missing information can materially change the interpretation, ask a focused question rather than inventing an assumption. Keep the result as ordinary, editable Python using Matplotlib, Seaborn, or Plotly.

Compare the signal and background score distributions for a paper. `weight` is the fixed per-event contribution to expected yield for the same target exposure. Preserve weighted yield; do not normalize either sample to unit area. Treat event contributions as independent for statistical uncertainty. The tail above score = 0.8 is scientifically important.

Save runnable plotting code as plot.py and render the result as plot.png.

OUTPUT CONTRACT:
- Write your complete, runnable plotting code directly to plot.py
- Execute plot.py to produce plot.png
- The first rendered figure is your attempt 1 submission
- Do NOT create intermediate test scripts or scratch files
